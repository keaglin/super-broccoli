geese.forEach(function(goose) {
	birds.filter(function(bird) {
		return bird !== goose
  })
})

var obj = {}

var wdiStudent = {
  name: 'Some Student',
  hasLaptop: true,
  'transit-mode': car,
  languages: ['javascript', 'ruby', 'python']
}
